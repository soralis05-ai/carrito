=================================================================
  CONFIGURACION DE GIT/GITHUB - ALMAPUNT CARRITO
=================================================================

REPOSITORIO REMOTO:
  Origin: https://github.com/soralis05-ai/carrito.git
  Rama:   main

TOKEN DE DESARROLLO:
  Estado: ✅ Guardado en Credential Manager de Windows
  Ubicacion: %USERPROFILE%\AppData\Local\Git\credentials
  Permisos: Lectura/Escritura (repo, workflow)

  El token NO está en ningún archivo .md ni .txt
  Está encriptado y solo Git puede acceder a él.

=================================================================
  COMANDOS PARA TRABAJAR DIARIAMENTE
=================================================================

# Antes de empezar a trabajar:
git pull origin main

# Después de hacer cambios:
git add .
git commit -m "Descripción del cambio"
git push origin main

# Ver estado:
git status

# Ver historial:
git log --oneline

=================================================================
  GESTION DE CREDENCIALES
=================================================================

Para ver, cambiar o eliminar el token guardado:

  scripts\git-credentials.bat

Este script te permite:
  1. Verificar que el token está guardado
  2. Ver el token (no recomendado en público)
  3. Eliminar el token (para cambiarlo)
  4. Verificar conexión con GitHub

=================================================================
  SI NECESITAS CAMBIAR EL TOKEN
=================================================================

1. Ejecuta: scripts\git-credentials.bat
2. Elige opción 3 (Eliminar credenciales)
3. La próxima vez que hagas "git push", Git te pedirá el nuevo token
4. Pega tu nuevo token de GitHub

O también:
  git config --global --unset credential.helper
  git config --global credential.helper store
  (Luego haz push e ingresa el nuevo token)

=================================================================
  TOKEN DE PRODUCCION (Solo lectura)
=================================================================

Para desplegar en producción (Vercel, Railway, etc.):

1. Crea un token NUEVO en GitHub:
   https://github.com/settings/tokens/new
   - Nombre: almapunt-production
   - Permisos: public_repo (solo lectura)

2. Configúralo en las variables de entorno del servidor:
   GITHUB_TOKEN=ghp_xxxxxxxxxxxxx

3. NUNCA uses tu token de desarrollo en producción

=================================================================
  UBICACION DE ARCHIVOS IMPORTANTES
=================================================================

Proyecto:       C:\soraya\carrito\
Git:            C:\Program Files\Git\bin\git.exe
Credenciales:   C:\Users\%USERNAME%\AppData\Local\Git\credentials
Config global:  C:\Users\%USERNAME%\.gitconfig

=================================================================
  SOPORTE
=================================================================

Documentación completa:
  - README.md (documentación del proyecto)
  - docs/GITHUB_SETUP.md (guía detallada de GitHub)
  - scripts/git-credentials.bat (gestión de tokens)

=================================================================
  ÚLTIMA ACTUALIZACIÓN: 28 de febrero de 2024
=================================================================
